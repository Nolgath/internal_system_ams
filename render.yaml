services:
  - type: web
    name: web-3-testing
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps chromium
    startCommand: python basic.py
