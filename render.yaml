services:
  - type: web
    name: internal-app-flask
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python basic.py
    plan: free